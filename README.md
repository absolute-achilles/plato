# Plato

E-Learning Management System

## Default credentials

A seed admin is created automatically on first startup via migrations:

- Email: `admin@plato.local`
- Password: `admin12345`

Change this password immediately after the first login via the change-password endpoint or UI.
